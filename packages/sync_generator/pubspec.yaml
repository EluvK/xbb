name: sync_generator
publish_to: none
description: Code generator for SyncModel
version: 0.0.1
resolution: workspace

environment:
  sdk: ^3.10.0

dependencies:
  sync_annotation:
    path: ../sync_annotation
  source_gen: ^4.1.1
  analyzer: '>=8.0.0 <9.0.0'

dev_dependencies:
  build_runner: ^2.10.4
