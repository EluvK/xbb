name: example
description: An example Flutter application for syncstore_client package.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  get: ^4.6.6
  sqflite: ^2.4.2
  sqflite_common_ffi: ^2.4.0
  syncstore_client:
    path: ../
